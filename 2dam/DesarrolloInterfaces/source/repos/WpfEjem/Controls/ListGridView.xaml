<UserControl x:Class="WpfEjem.ListGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfEjem"
             xmlns:vm="clr-namespace:WpfEjem.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:ListGridViewModel />
    </UserControl.DataContext>
    <Grid>
        <!--ListView con GridView para mostrar datos en formato tabla -->
        <ListView ItemsSource="{Binding Languages}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Nombre" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Tipo" DisplayMemberBinding="{Binding Type}" />
                    <GridViewColumn Header="Año de Creación" DisplayMemberBinding="{Binding CreationYear}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
