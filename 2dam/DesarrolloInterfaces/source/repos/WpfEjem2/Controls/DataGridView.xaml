<UserControl x:Class="WpfEjem.DataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfEjem"
             xmlns:vm="clr-namespace:WpfEjem.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:ListGridViewModel />
    </UserControl.DataContext>
    <Grid>
        <!--DataGrid para mostrar la colección de lenguajes -->
        <DataGrid ItemsSource="{Binding Languages}"
                  AutoGenerateColumns="True"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Tipo" Binding="{Binding Type}" />
                <DataGridTextColumn Header="Año de Creación" Binding="{Binding CreationYear}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
