<UserControl x:Class="WpfEjem2.CustomListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfEjem2"
             xmlns:vm="clr-namespace:WpfEjem2.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:CustomListViewModel />
    </UserControl.DataContext>
    <Grid>
        <!--ListView con plantilla personalizada para los elementos -->
        <ListView ItemsSource="{Binding Languages}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="5">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding ImageUri}" Width="50" Height="50" Margin="0,0,10,0" />
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                <TextBlock Text="{Binding Type}" />
                                <TextBlock Text="{Binding CreationYear}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
